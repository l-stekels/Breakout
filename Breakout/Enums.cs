namespace Breakout
{
    public enum GameState
    {
        Active,
        Menu,
        Win,
        Pause
    }

    public enum Direction
    {
        Up,
        Right,
        Down,
        Left
    }
}
